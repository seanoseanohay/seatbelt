{
  "name": "seatbelt-harness-owned-timing",
  "version": "0.0.1",
  "description": "Spike 003: Validation that the *harness* (not the model) owns review timing and promotion gates. No voluntary self-review escape hatch.",
  "type": "module",
  "scripts": {
    "start": "tsx src/validate-harness-owned.ts",
    "openai": "BACKEND=openai tsx src/validate-harness-owned.ts",
    "codex": "BACKEND=codex-cli tsx src/validate-harness-owned.ts",
    "messy": "BACKEND=codex-cli tsx src/validate-harness-owned.ts"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "latest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
