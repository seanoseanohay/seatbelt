# === Option A: Normal OpenAI API key (recommended for pi-ai) ===
OPENAI_API_KEY=sk-...

# === Option B: Codex CLI (uses ChatGPT login) ===
# Run `codex login` once. The harness can then call `codex exec`.
# No key needed here — it uses ~/.codex/auth.json
